/** Event branding PNG (shown next to GP logo everywhere except /idle). */
export const EVENT_LOGO_PNG = "/EventLogo.png";

/** Grand Prix mark — paired with event logo on most screens; alone on /idle. */
export const GP_LOGO_PNG = "/GPLOGO.png";
